/**
 * Deployment bootstrap: allows first sign-in when the database has no users yet.
 * After first login, change this password from the app settings / DB.
 */
export const DEPLOY_BOOTSTRAP_EMAIL = "admin@codingforge.com";
export const DEPLOY_BOOTSTRAP_PASSWORD = "12345678";
